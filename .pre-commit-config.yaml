# pip install pre-commit && pre-commit install
# Runs the same ruff version and config as .github/workflows/ci.yml's lint job.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.21
    hooks:
      - id: ruff
        args: [--exit-non-zero-on-fix]
